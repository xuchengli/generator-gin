package main

import (
	"<%= project_name %>/application"
	"<%= project_name %>/config"
	"<%= project_name %>/log"
	"<%= project_name %>/router"
)

func main() {
	cfg := config.LoadConfig()

	// 初始化日志格式
	log.Init(cfg)

	// 初始化路由
	r := router.InitRoutes(application.InitContext(cfg))

	r.Run()
}
